{
  "name": "iper",
  "version": "0.3.1",
  "engines": {
    "node": ">=4.2.3"
  },
  "description": "Hypergraphs for breakfast",
  "author": {
    "name": "fibo",
    "url": "http://g14n.info"
  },
  "main": "index.js",
  "scripts": {
    "_build": "npm test && npm run browserify && npm run tex && npm run jekyll; git status",
    "_pull": "git pull origin master && npm run gh-pages:pull",
    "_push": "git push origin master && npm run gh-pages:push",
    "bower.json": "npm run bower.json:version; npm run bower.json:commit",
    "bower.json:commit": "git commit -am 'sync with package.json'; git tag -f $npm_package_version",
    "bower.json:version": "perl -pi -w -e 's/\"version\": \"[\\d\\.]+\"/\"version\": \"$ENV{npm_package_version}\"/' bower.json",
    "browserify": "npm run browserify:dist; npm run browserify:test",
    "browserify:dist": "browserify -r ./index.js:${npm_package_name} -t babelify -o dist/${npm_package_name}.js",
    "browserify:test": "browserify test/*.js -t babelify -o gh-pages/test/bundle.js",
    "coveralls": "cat ./coverage/lcov.info | coveralls --verbose",
    "coverage": "npm run istanbul && npm run coveralls",
    "decrypt:.coveralls.yml": "keybase decrypt -o .coveralls.yml .coveralls.yml.asc",
    "dox": "cat src/*.js | dox > gh-pages/_data/dox.json",
    "homepage": "echo \"---\ntitle: $npm_package_name\n---\" > gh-pages/index.md; cat README.md >> gh-pages/index.md",
    "gh-pages:_data": "npm run package.json; npm run dox",
    "gh-pages:push": "git subtree --prefix gh-pages push origin gh-pages",
    "gh-pages:pull": "git subtree --prefix gh-pages pull origin gh-pages",
    "istanbul": "istanbul cover _mocha",
    "jekyll": "npm run homepage; npm run gh-pages:_data; cd gh-pages; jekyll build; cd ..",
    "jshint": "jshint -c .jshintrc src/*.js",
    "mocha": "mocha",
    "package.json": "cp package.json gh-pages/_data",
    "postversion": "npm run bower.json; git push origin v${npm_package_version}; npm publish",
    "tex": "npm run tex:display; npm run tex:inline",
    "tex:display": "ls gh-pages/tex-snippets/display/*.tex | while read texfile; do cat $texfile | katex --display-mode > gh-pages/_includes/display/$(basename ${texfile%.tex}).html; done",
    "tex:inline": "ls gh-pages/tex-snippets/inline/*.tex | while read texfile; do cat $texfile | katex > gh-pages/_includes/inline/$(basename ${texfile%.tex}).html; done",
    "test": "npm run jshint; npm run mocha"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/fibo/iper.git"
  },
  "keywords": [
    "math",
    "graph"
  ],
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/fibo/iper/issues"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.3.13",
    "babelify": "^7.2.0",
    "browserify": "^12.0.1",
    "coveralls": "^2.11.6",
    "dox": "^0.8.0",
    "istanbul": "^0.4.1",
    "jshint": "^2.8.0",
    "katex": "^0.5.1",
    "mocha": "^2.3.4",
    "should": "^8.0.0"
  },
  "dependencies": {
    "lodash.uniqueid": "^3.0.0",
    "strict-mode": "^0.4.1"
  },
  "homepage": "http://www.g14n.info/iper"
}
